{
  "name": "cosmwasm-tools",
  "license": "MIT",
  "scripts": {
    "gents": "yarn -s ts-node gents.ts",
    "build": "yarn -s ts-node build.ts",
    "postinstall": "cd node_modules/@cosmwasm/ts-codegen && yarn && yarn build"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "@cosmwasm/ts-codegen": "https://github.com/oraichain/ts-codegen.git",
    "typescript-parser": "^2.6.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  }
}
